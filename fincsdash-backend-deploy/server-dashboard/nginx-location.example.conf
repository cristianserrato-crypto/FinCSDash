# Opción A — subdominio dedicado (recomendado, no toca fincsdash.online)
# server { listen 80; server_name dash.tudominio.com;
#   location / {
#     proxy_pass http://127.0.0.1:8001;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
# }

# Opción B — mismo server_name, otra ruta (menos limpio)
# location /sysdash/ {
#   proxy_pass http://127.0.0.1:8001/;
#   proxy_set_header Host $host;
#   proxy_set_header X-Real-IP $remote_addr;
#   proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#   proxy_set_header X-Forwarded-Proto $scheme;
# }
# Nota: con Opción B habría que ajustar el front para base path /sysdash/ (no incluido por defecto).
